.data
.DS_Store

# Quackage-generated build scaffolding (regenerated by `npx quack build`)
.babelrc
.browserslistrc
.browserslistrc-BACKUP
.gulpfile-quackage-config.json
.gulpfile-quackage.js
.package.json.quackage.bak
*.quackage.bak

package-lock.json

# Logs
logs
*.log
npm-debug.log*

# Coverage
coverage
*.lcov
.nyc_output

# Dependency directories
node_modules/

# Build output
dist

# Output of 'npm pack'
*.tgz

# dotenv
.env
.env.local

# Sample Plan harness — LOCAL-ONLY verification app built from real LADOTD-derived fixtures.
# NEVER commit: it carries real TestDescription/SamplingInstructions/Intent text, real IDs, and a real
# project code. The generic explorer features (RowMeters, node-selection) live in source/ and DO ship;
# the customer-data harness does not. The Sample Plan editor proper lives in the (private) headlight repo.
example_applications/sampleplan_explorer/
